package routing

// stable_first.go implements the stable_first routing strategy helpers.
// The core logic (pool plan, weighted selection, site rotation) is in weights.go.
// This file provides observation gating and integration glue.
